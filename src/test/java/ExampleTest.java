import org.junit.Test;

import static org.hamcrest.MatcherAssert.assertThat;
import static org.hamcrest.Matchers.is;

public class ExampleTest {
    @Test
    public void test_framework() throws Exception {
        assertThat(1,is(1));
    }
}
